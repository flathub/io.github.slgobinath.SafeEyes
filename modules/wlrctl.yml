name: wlrctl
buildsystem: meson
sources:
  - type: git
    url: "https://git.sr.ht/~brocellous/wlrctl"
    commit: 3986d4e4d9075be3418043a8e053d7531f22558f # until new tag
    # tag: v0.2.2
    # x-checker-data:
    #   type: git
    #   tag-pattern: "^v([\\d.]+)$"
# only-arches: # can be deleted after new tag
#   - x86_64